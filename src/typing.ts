//función anónima autoejecutada
(()=>{

})()

let myProductName: string = 'ChocOlate';

let myproductPrice: number = 123;

let minus = myProductName.toLowerCase(); //.lowerCase no tiene hace mucho, no modifica la variable original

console.log(minus);

const myProductStock = 1000; // no podes reasignar el valor de una constante

const jose = '123'; // las constantes son de tipo constante XD


// es necesario hacerlo por inferencia todo el tiempo? preferiría que no, esas variables ya están definidas omeee
// alcances, programación modular, eso siempre me ha parecido muy chistoso, ejecutar la función apenas entras, muy curioso
