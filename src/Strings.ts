(() => {
  let productTitle = 'Amazing product jai papá';
  productTitle = 'New title';

  console.log(productTitle);

  // se prefiere usar comillas simples, los apostrofes, es la única vez cuando es necesario usar comillas dobles
  const productDescription = "HOLAA, soy' una descripcion";

  // Enters y espacios :0
  const summary = `title: ${productTitle}
   description: ${productDescription}`;

  console.log(summary);

  const myString: string = 'hello, mafren';
})();
